# QA CHECKLIST — SARMAT DOORS READY15 FINAL PRINT FIX

Проверено перед упаковкой:

- [x] Python `server.py` компилируется без синтаксических ошибок.
- [x] JavaScript inline-скрипт `index.html` проходит `node --check`.
- [x] Все ключевые кнопки имеют обработчики: сформировать КП, печать, скачать PDF, сформировать заказ.
- [x] Все обязательные DOM id присутствуют.
- [x] В проекте нет `checkpoint`.
- [x] В PDF нет общего текста «толщина указывается...».
- [x] В каждой строке PDF фактические `leafMm` и `frameMm` берутся из конкретной позиции.
- [x] `/api/health` отвечает JSON.
- [x] Пустой заказ `/api/pdf` возвращает понятную ошибку.
- [x] `/api/final-pdf` не выдаёт финальный PDF до успешной Telegram-отправки.
- [x] Обычный PDF формируется как A4.
- [x] Обычный PDF не содержит подписи/печати.
- [x] Обычный PDF не содержит маркировки ЭТАП 1/ЭТАП 2.
- [x] Финальный PDF содержит подпись, печать и маркировку ЭТАП 3.
- [x] Проверен тест с 50 позициями: PDF сформирован в 9 страниц, заголовок таблицы повторяется, условия находятся после последней строки.
- [x] Проверена визуальная страница PDF: шапка, таблица, золотые ценовые колонки, A4 и подвал.
- [x] Печать вынесена в отдельный HTML-макет, поэтому печатается КП, а не весь сайт.
- [x] Для печатного макета предусмотрено автоматическое разбиение на несколько страниц с повторением шапки/подвала.

## Этапы пользователя

**До отправки:** КП без подписи и печати.

**После успешной отправки в Telegram:** финальное КП с подписью и печатью.
